schema {
  query: MyAPIQuery
}

type Film {
  id: String!
  title: String!
  director: String!
}

type FilmList {
  films: [Film!]!
}

type MyAPIQuery {
  allFilms: FilmList!
  ping(message: String!): String!
}
